


{
  "name": "videoloader",
  "version": "2.0.0",
  "main": "server.js",
  "scripts": {
    "start": "apt-get update && apt-get install -y python3 python3-pip ffmpeg && pip3 install yt-dlp && node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "youtube-dl-exec": "^3.0.0"
  }
}
